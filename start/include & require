include 
	首先按照include给定的参数进行寻找文件，如果没有找到，那么按include_path 指定的目录寻找。如果还是没有找到，那么会调用
	脚本文件所在的目录和当前的工作目录查找。如果还是没有找到，那么最后会发出一个警告（require 会发出一个致命的错误）
	如果以 ../的格式进行查找，那么include_path 会被忽略，而在文件的父目录中查找
require 
	和 include 几乎一样，只是require发出的是致命的错误

include_once 
	只包含一次。如果文件可能被包含多次。避免函数重定义，变量新赋值等情况。
	